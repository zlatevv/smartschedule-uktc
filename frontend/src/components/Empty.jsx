import { C } from "../constants";

export default function Empty() {
    return (
        <span style={{ color: C.faint, fontFamily: "Fira Code, monospace", fontSize: 11 }}>
      —
    </span>
    );
}
